# Release Notes for tka navigation

## dev-main
- Added: Settings option to toggle between editor interfaces ("Slick Lean" compact inline vs "Easy" sidebar inspector).
- Added: Two distinct visual workspace sub-templates (`_edit_slick.twig` and `_edit_easy.twig`) selectable dynamically via Control Panel configuration.
- Fixed: Resolved `forms` macro scope boundary issue within nested include templates.

## 1.0.0
- Initial release
